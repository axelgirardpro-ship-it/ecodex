// Ajouts nécessaires à Login.tsx :

1. Import de PasswordReset :
import { PasswordReset } from "@/components/auth/PasswordReset";

2. État pour gérer l'affichage du reset :
const [showPasswordReset, setShowPasswordReset] = useState(false);

3. Condition dans le render pour afficher PasswordReset ou LoginForm

4. Ajouter le lien "Mot de passe oublié" après le bouton de connexion :

              <div className="text-center mt-2">
                <button
                  type="button"
                  onClick={() => setShowPasswordReset(true)}
                  className="text-sm text-blue-600 hover:underline"
                >
                  Mot de passe oublié ?
                </button>
              </div>
